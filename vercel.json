{
  "buildCommand": "pnpm --filter @brepflow/studio^... run build && pnpm --filter @brepflow/studio run build && cp -r apps/studio/dist ./dist",
  "installCommand": "pnpm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/functions/studio"
    }
  ],
  "functions": {
    "functions/studio.js": {
      "maxDuration": 30
    }
  }
}