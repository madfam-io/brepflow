{
  "buildCommand": "pnpm --filter @brepflow/studio^... run build && pnpm --filter @brepflow/studio run build && mkdir -p dist && cp -r apps/studio/dist/* dist/",
  "installCommand": "pnpm install",
  "outputDirectory": "dist",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/functions/studio.js"
    }
  ],
  "functions": {
    "functions/studio.js": {
      "maxDuration": 30
    }
  }
}